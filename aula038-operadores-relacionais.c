#include <stdio.h>
#include <stdlib.h>

/*
        Aula 38: Operadores relacionais
            menor que (a < b)       => 1
            maior que (a > b)       => 0
            menor ou igual (a <= b) => 1
            maior ou igual (a >= b) => 0
            igual (a == b)          => 0    / 1
            diferente (a != b)      => 1    / 0

*/
int main() {

    int a = 10, b = 20;

    printf("\n\tResultado: %d\n\n", a != b);
    return 0;
}
